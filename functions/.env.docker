# Docker Environment for Local Testing
# This is a test configuration - replace with actual values

NODE_ENV=production
PORT=8080
HOST=0.0.0.0

APP_VERSION=1.0.0-local
LOG_LEVEL=info

CORS_ORIGIN=http://localhost:3000,http://localhost:8080

# GCP/Firebase Configuration (placeholder values)
GOOGLE_CLOUD_PROJECT=cortex-dc-project
FIREBASE_PROJECT_ID=cortex-dc-project
FIREBASE_DATABASE_URL=https://cortex-dc-project.firebaseio.com

# Genkit AI (placeholder - replace with real key)
GOOGLE_GENAI_API_KEY=test-key-replace-with-real-value
