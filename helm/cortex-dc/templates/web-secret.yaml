{{- if .Values.web.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: cortex-web-secrets
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "cortex-dc.labels" . | nindent 4 }}
    {{- include "cortex-dc.web.selectorLabels" . | nindent 4 }}
type: Opaque
stringData:
  # Secrets are managed via Google Secret Manager or external secrets operator
  # This is a placeholder for structure
  PLACEHOLDER: "see-google-secret-manager"
{{- end }}
