apiVersion: v1
kind: ServiceAccount
metadata:
  name: cortex-web-sa
  namespace: cortex-dc
  labels:
    app: cortex-web
    component: frontend
  annotations:
    # GKE Workload Identity (if using GCP)
    iam.gke.io/gcp-service-account: cortex-web@cortex-dc-project.iam.gserviceaccount.com
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: cortex-web-role
  namespace: cortex-dc
rules:
- apiGroups: [""]
  resources: ["configmaps", "secrets"]
  verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: cortex-web-rolebinding
  namespace: cortex-dc
subjects:
- kind: ServiceAccount
  name: cortex-web-sa
  namespace: cortex-dc
roleRef:
  kind: Role
  name: cortex-web-role
  apiGroup: rbac.authorization.k8s.io
